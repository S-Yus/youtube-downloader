# docker compose 用の環境変数。 .env にコピーして値を設定する:
#   cp .env.example .env
# パスフレーズ生成: python -c "import secrets; print(secrets.token_urlsafe(24))"
# SECRET_KEY生成:  python -c "import secrets; print(secrets.token_hex(32))"
APP_USERNAME=yourname
APP_PASSWORD=
SECRET_KEY=
